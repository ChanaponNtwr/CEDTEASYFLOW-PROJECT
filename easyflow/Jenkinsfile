pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'cedt-easyflow'
        DOCKER_TAG = "${env.BUILD_NUMBER}"

        // Git config (ตั้งค่าให้ตรงกับ Jenkins credentials ที่คุณสร้าง)
        GIT_URL = 'https://github.com/ChanaponNtwr/CEDTEASYFLOW-PROJECT.git'
        GIT_BRANCH = 'main'
        GIT_CREDENTIALS_ID = 'github-token' // <-- ใส่ Jenkins credentials id ที่สร้างไว้
    }

    stages {
        stage('Checkout') {
            steps {
                dir('easyflow') {
                    // ใช้ Jenkins git step — จะ clone ครั้งแรกและ update ครั้งถัดไปให้อัตโนมัติ
                    git branch: "${GIT_BRANCH}",
                        url: "${GIT_URL}",
                        credentialsId: "${GIT_CREDENTIALS_ID}"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                dir('easyflow') {
                    script {
                        echo "Building Docker image ${DOCKER_IMAGE}:${DOCKER_TAG}..."
                        if (isUnix()) {
                            sh 'docker build -t ${DOCKER_IMAGE}:${DOCKER_TAG} .'
                            sh 'docker tag ${DOCKER_IMAGE}:${DOCKER_TAG} ${DOCKER_IMAGE}:latest'
                        } else {
                            bat "docker build -t %DOCKER_IMAGE%:%DOCKER_TAG% ."
                            bat "docker tag %DOCKER_IMAGE%:%DOCKER_TAG% %DOCKER_IMAGE%:latest"
                        }
                    }
                }
            }
        }

        stage('Test / Validate') {
            steps {
                dir('easyflow') {
                    script {
                        echo "Running basic tests or validation..."
                        // sh 'npm run test'
                    }
                }
            }
        }

        stage('Deploy Container') {
            steps {
                script {
                    echo "Deploying Docker container ${DOCKER_IMAGE}:${DOCKER_TAG}..."
                    if (isUnix()) {
                        sh '''
                        docker rm -f cedt-easyflow 2>/dev/null || true
                        docker run -d --name cedt-easyflow -p 3000:3000 ${DOCKER_IMAGE}:${DOCKER_TAG}
                        '''
                    } else {
                        bat """
                        docker rm cedt-easyflow || echo No existing cedt-easyflow container
                        docker run -d --name cedt-easyflow -p 3000:3000 %DOCKER_IMAGE%:%DOCKER_TAG%
                        """
                    }
                }
            }
        }
    }

    post {
        always {
            echo "Pipeline finished. Cleaning workspace..."
            cleanWs()
        }
        success {
            echo "Pipeline succeeded!"
        }
        failure {
            echo "Pipeline failed! Please check logs."
        }
    }
}