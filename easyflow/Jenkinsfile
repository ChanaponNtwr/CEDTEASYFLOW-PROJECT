pipeline {
  agent any
  environment {
    IMAGE_NAME = "easyflow-frontend:${env.BUILD_NUMBER}"
  }

  stages {

    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Install & Test & Build (node)') {
      agent {
        docker {
          # --- ส่วนที่แก้ไข (FIX) ---
          # เปลี่ยนจาก node:18 เป็น node:20 เพื่อให้รองรับ Prisma 7.x และ Next.js 15 ได้เต็มประสิทธิภาพ
          image 'node:20' 
          # ------------------------
          args '-u root:root -e BASE_API=${BASE_API} -e NODE_ENV=development'
        }
      }
      steps {
        dir('easyflow') {
          sh '''
             # 1. เตรียม Schema
             echo "=== SETUP SCHEMA FROM BACKEND ==="
             mkdir -p prisma
             cp ../backend/prisma/schema.prisma ./prisma/schema.prisma

             # 2. Clean Install
             echo "=== CLEAN & INSTALL ==="
             rm -rf node_modules
             rm -rf .prisma
             rm -rf node_modules/.prisma
             rm -f package-lock.json
             
             # 3. Update Prisma เป็นเวอร์ชันล่าสุด (ตอนนี้จะผ่านแล้วเพราะใช้ Node 20)
             echo "=== UPDATE PRISMA & INSTALL DEPENDENCIES ==="
             # ติดตั้ง dependencies และบังคับใช้ Prisma ล่าสุด
             npm install prisma@latest @prisma/client@latest --save-exact

             echo "=== GENERATE & BUILD ==="
             
             # Generate Client
             npx prisma generate --schema=./prisma/schema.prisma

             npm test || true
             
             echo "Checking BASE_API: $BASE_API"
             npm run build
          '''
        }
      }
    }
    
    stage('Build Docker Image') {
      steps {
        sh '''
          # ตอน Build Docker Image จริง ก็ควรเปลี่ยนเป็น node:20 ด้วยเพื่อให้ Environment ตรงกัน
          # แต่ถ้า Dockerfile ของคุณ fix ไว้ที่ 18 อาจจะแก้ในนี้ไม่ได้ทันที (แต่ production รันได้)
          # แนะนำให้ไปแก้ไฟล์ Dockerfile ในโปรเจกต์ด้วยถ้าทำได้ แต่ถ้าไม่ได้ ให้ใช้บรรทัดนี้ไปก่อน
          
          docker build \
            -t easyflow-frontend:${BUILD_NUMBER} \
            -f easyflow/Dockerfile \
            .
          docker tag easyflow-frontend:${BUILD_NUMBER} easyflow-frontend:latest
        '''
      }
    }

    stage('Deploy (local docker)') {
      steps {
        sh """
          docker stop easyflow-frontend || true
          docker rm easyflow-frontend || true
          
          docker run -d \\
            --name easyflow-frontend \\
            -p 3000:3000 \\
            --link postgres-db:postgres \\
            --link easyflow-backend:backend \\
            -e DATABASE_URL="postgresql://postgres:root@postgres:5432/postgres" \\
            -e BASE_API="http://easyflow-backend:9000" \\
            -e NEXTAUTH_URL="http://localhost:3000" \\
            -e NEXTAUTH_SECRET="change-me-to-secure-random-string" \\
            -e SERVER_HOST="0.0.0.0" \\
            -e GOOGLE_CLIENT_ID="your-real-id-or-leave-empty-if-not-used" \\
            -e GOOGLE_CLIENT_SECRET="your-real-secret-or-leave-empty-if-not-used" \\
            easyflow-frontend:latest
        """
      }
    }
  }

  post {
    always {
      archiveArtifacts artifacts: 'easyflow/.next/**', allowEmptyArchive: true
    }
  }
}